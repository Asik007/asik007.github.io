---
interface Props {
  title: string;
  id?: string;
}

const { title, id } = Astro.props;
---

<a class="anchor" id={id}></a>
<div class="cv-card">
  <h3 class="card-title font-weight-medium">{title}</h3>
  <div>
    <slot />
  </div>
</div>
